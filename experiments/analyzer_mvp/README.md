# Prototype: Analyzer MVP

This folder contains prototype materials and wireframes used during early product discovery. The active Analyzer implementation lives in `src/analyzer`.

Recommendations:

- Archive or move this entire folder to `experiments/archive/` once the prototype artifacts are no longer needed.
- If you need to keep specific artifacts (wireframes, notes), keep them under `experiments/archive/analyzer_mvp` with a short README describing the content.
- Avoid editing `src/analyzer` and files in `experiments` simultaneously; prefer `src/analyzer` as the canonical source of production code.

If you want help migrating prototype code into `src/analyzer`, we can create a migration plan or PR to copy relevant components and then archive the old ones.
